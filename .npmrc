# Intentionally minimal. Vercel builds the web app remotely with its native
# pnpm-workspace support (default linker), so no hoisting is needed here.
# (Hoisting caused duplicate React copies across the web(19)/mobile(18) workspace.)
