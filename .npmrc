# Flat node_modules so Next.js output-file-tracing + `vercel deploy --prebuilt`
# include real dependency files (fixes ENOENT on .pnpm symlinks, e.g. styled-jsx).
node-linker=hoisted
shamefully-hoist=true
